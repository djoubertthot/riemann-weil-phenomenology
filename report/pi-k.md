# Π_k, first diagnostic

`code/pi_k.py` — four mock sectors on the *radial*
Fourier (sign patterns on the lacunary n). Not the
true projectors on ℤ₂^*.

| sector | N=80 ∑λ² | N=160 ∑λ² |
|--------|----------|-----------|
| triv | 2.64 | 3.26 |
| k=2 | 3.22 | 3.68 |
| k=3 | 3.94 | 4.45 |
| k=4 | 3.94 | 4.45 |

k=3 and k=4 coincide: the mock phases are linearly
dependent on n. The log is **not** confined to the
trivial sector — every pattern grows with N.

What this kills: « the divergence is only the
invariant mode, subtract c log ». What it does not
give: a VP. Next real step is characters of
U₂/U_K as a second axis, not more sign flips on n.
