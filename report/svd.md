# SVD — χ₂₉ μ=11

Φ is 52 zeros × 25 modes. Singular values of Φ
are flat (~1.9 … 1.7): the hats fill the window.

G − Q is essentially **rank 1**:

    σ = 1.615, 0.057, 0.0009, 0, 0, …

σ₀² / ∑σ² = 0.999. Nuclear ≈ operator ≈ F-norm
≈ 1.62.

The 6 % Frobenius error is one direction.
Together with overlap(v0)=0.999, that direction
is a scale mismatch on a mode already shared,
plus a sliver of mode 6 — not a full disagreement
of Weil matrices.
